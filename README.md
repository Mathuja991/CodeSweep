# CodeSweep Website

This folder is a standalone static showcase site built with HTML, CSS, and JavaScript.

## Local preview

```powershell
cd website
start index.html
```

## Vercel

Set the Vercel Root Directory to `website` and deploy it as a static site.
